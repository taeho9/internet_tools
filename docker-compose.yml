version: "3.8"

services:
  nginx:
    build:
      context: ./nginx
    image: tools-nginx:latest
    container_name: tools-blogger-nginx
    volumes:
      - /data/tools:/var/www/html
    environment:
      - TZ=Asia/Seoul
    expose:
      - "80"
    restart: unless-stopped
    networks:
      - reverse_proxy
      - internal_network
    depends_on:
      - php-fpm

  php-fpm:
    build:
      context: ./php-fpm
    image: tools-php-fpm:latest
    container_name: tools-blogger-php-fpm
    volumes:
      - /data/tools:/var/www/html
      - /data/tools/logs:/var/www/html/logs
    environment:
      - TZ=Asia/Seoul
    restart: unless-stopped
    networks:
      - internal_network

networks:
  reverse_proxy:
    external: true
  internal_network:
    driver: bridge